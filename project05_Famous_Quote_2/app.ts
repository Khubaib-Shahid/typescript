let famous_person : string = `Quaid-e-Azam`;

let message : string = `${famous_person} once said, "Before you make a decision… “Think a hundred times before you take a decision, but once that decision is taken, stand by it as one man.”"`;

console.log(message);
